> Install: `pipx install git+https://github.com/initstring/cloud_enum`

Run `cloud_enum -k <company-name> -k <domain>`; check passes if no publicly accessible cloud storage referencing the company is discovered.
