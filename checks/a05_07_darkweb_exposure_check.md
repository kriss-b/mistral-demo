> Install: `brew install tor` and `pip install darkdump`

Start Tor with `tor`, then run `darkdump --query <company-name>` and `darkdump --query <domain>`; check passes if no results reference the company's internal data, credentials, or documents.
